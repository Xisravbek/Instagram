const Posts = require("../model/postsModel")

const postCtrl = {
    addPost: async (req, res ) => {
        try {
            const userId = req.user._id;
            
        } catch (error) {
            
        }
    }
}
